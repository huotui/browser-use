# Browser-Use 详细开发文档

## 目录

1. [系统概述](#系统概述)
2. [核心架构](#核心架构)
3. [组件详解](#组件详解)
4. [核心数据流与工作流](#核心数据流与工作流)
5. [LLM集成体系](#llm集成体系)
6. [DOM处理系统](#dom处理系统)
7. [GUI架构](#gui架构)
8. [部署与打包](#部署与打包)
9. [开发指南](#开发指南)

---

## 系统概述

### 项目简介

Browser-Use 是一个基于大语言模型驱动的智能浏览器自动化工具，通过集成 Chrome DevTools Protocol (CDP) 实现浏览器的完全控制，利用 LLM 的推理能力自动完成复杂的网页浏览任务。

### 核心特性

- 多 LLM 提供商支持（OpenAI、Anthropic、本地 vLLM 等）
- 完整的 Chrome 浏览器自动化控制
- 可访问性树生成与智能元素识别
- 图形化配置与任务执行界面
- 打包为独立可执行文件分发

### 技术栈概览

| 类别 | 技术选型 |
|------|----------|
| 浏览器控制 | Chrome DevTools Protocol (CDP) |
| 事件驱动 | bubus 事件总线 |
| 异步框架 | asyncio |
| 数据验证 | Pydantic v2 |
| GUI | Tkinter |
| 打包工具 | PyInstaller |
| HTTP 客户端 | aiohttp |

---

## 核心架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                     GUI 层 (Tkinter)                         │
│  ┌───────────────┐  ┌───────────────┐  ┌─────────────────┐  │
│  │ 配置选项卡    │  │ 任务选项卡    │  │ 日志输出面板   │  │
│  └───────────────┘  └───────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   Agent 服务层                                │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Agent.run() 主循环 - 决策 → 动作 → 观察 → 思考        │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  LLM 集成层                                  │
│  ┌───────────────┐  ┌───────────────┐  ┌─────────────────┐  │
│  │ BaseChatModel│  │  提供商实现    │  │  工厂函数       │  │
│  └───────────────┘  └───────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                DOM 处理服务层                                │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 页面快照 →  accessibility tree → 元素索引 → LLM 优化   │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│              浏览器会话管理层                                │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ BrowserSession - CDP 通信 + 事件处理 + Watchdog       │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 组件详解

### 1. Agent 核心服务

**文件位置**: `browser_use/agent/service.py`

#### 核心类与方法

```python
class Agent:
    async def run(self, max_steps: int = 100):
        """
        Agent 主执行循环
        
        执行流程：
        1. 初始化状态
        2. 循环执行直到任务完成或达到最大步数
        3. 每步：获取页面状态 → 调用 LLM → 执行动作 → 更新状态
        """
```

#### 执行流程图

```
开始
  │
  ▼
初始化Agent状态
  │
  ▼
┌────────────────────────────────────┐
│         主循环 (max_steps次)      │
│  ┌─────────────────────────────┐  │
│  │ 1. 获取浏览器页面快照        │  │
│  └─────────────────────────────┘  │
│  ┌─────────────────────────────┐  │
│  │ 2. DOM处理生成可访问性树     │  │
│  └─────────────────────────────┘  │
│  ┌─────────────────────────────┐  │
│  │ 3. 构建LLM提示词             │  │
│  └─────────────────────────────┘  │
│  ┌─────────────────────────────┐  │
│  │ 4. 调用大模型决策            │  │
│  └─────────────────────────────┘  │
│  ┌─────────────────────────────┐  │
│  │ 5. 解析LLM输出为动作         │  │
│  └─────────────────────────────┘  │
│  ┌─────────────────────────────┐  │
│  │ 6. 在浏览器中执行动作        │  │
│  └─────────────────────────────┘  │
│  ┌─────────────────────────────┐  │
│  │ 7. 检查任务是否完成          │  │
│  └─────────────────────────────┘  │
│         │ 否 │                     │
└─────────循环───────────────────────┘
          │ 是 │
          ▼
任务完成，返回结果
```

### 2. 浏览器会话管理

**文件位置**: `browser_use/browser/session.py`

#### BrowserSession 核心特性

- **CDP 通信**: 基于 WebSocket 与 Chrome 进行双向通信
- **事件总线**: 使用 bubus 实现发布-订阅模式
- **Watchdog 模式**: 监控浏览器会话健康状态
- **生命周期管理**: 浏览器进程启动、监控、优雅关闭

#### 关键配置项

```python
# 浏览器初始化参数配置
browser_args = [
    '--no-first-run',
    '--no-default-browser-check',
    '--disable-infobars',
    '--disable-popup-blocking',
]
```

### 3. DOM 处理服务

**文件位置**: `browser_use/dom/service.py`

#### 处理流水线

```
原始HTML页面
     │
     ▼
DOM序列化提取
     │
     ▼
可访问性树(Accessibility Tree)生成
     │
     ▼
元素索引与编号
     │
     ▼
LLM优化格式输出
```

#### 核心能力

- 自动过滤不可见元素
- 为交互元素分配唯一索引编号
- 生成适合 LLM 阅读的简化 DOM 表示
- 支持通过索引快速定位和操作元素

---

## 核心数据流与工作流

### Agent 完整执行工作流

```
用户输入任务
      │
      ▼
GUI层启动Agent (新线程)
      │
      ▼
创建异步事件循环
      │
      ▼
初始化Browser → 启动Chrome进程
      │
      ▼
初始化BrowserSession → 建立CDP连接
      │
      ▼
Agent.run() 开始执行
      │
      ├──────────────────────────────────┐
      │                                  │
      ▼                                  │
获取当前页面快照                        │
      │                                  │
      ▼                                  │
DOM服务处理 → 生成可访问性树            │
      │                                  │
      ▼                                  │
构建系统提示词 + 用户任务 + 页面状态    │
      │                                  │
      ▼                                  │
LLM调用 (ainvoke)                       │
      │                                  │
      ▼                                  │
解析LLM响应 → 动作列表                 │
      │                                  │
      ▼                                  │
在浏览器中执行点击/输入/滚动等操作      │
      │                                  │
      ▼                                  │
等待页面稳定/加载完成                   │
      │                                  │
      └────────────── 循环 ──────────────┘
             │
             ▼
LLM判定任务完成 → 返回结果
             │
             ▼
GUI层更新显示结果与日志
```

---

## LLM 集成体系

### 架构设计模式

采用抽象工厂模式统一管理多提供商 LLM 集成。

### BaseChatModel 抽象基类

**文件位置**: `browser_use/llm/base.py`

定义所有 LLM 实现必须遵循的统一接口：

```python
class BaseChatModel(ABC):
    @abstractmethod
    async def ainvoke(
        self,
        messages: list[dict[str, Any]],
        temperature: float = 0.7,
        max_tokens: int = 4096,
        **kwargs
    ) -> dict[str, Any]:
        """异步调用大模型"""
        pass
    
    @abstractmethod
    async def ainvoke_batch(
        self,
        batch_messages: list[list[dict[str, Any]]],
        **kwargs
    ) -> list[dict[str, Any]]:
        """批量异步调用"""
        pass
    
    @abstractmethod
    def get_model_info(self) -> dict[str, str]:
        """获取模型元信息"""
        pass
```

### 工厂函数实现

**文件位置**: `browser_use/llm/models.py`

```python
def get_llm_by_name(model_name: str) -> BaseChatModel:
    """
    根据模型名称创建对应的LLM实例
    
    支持模型列表：
    - OpenAI 系列
    - Anthropic Claude 系列
    - 本地 vLLM 部署系列
    """
```

### 已支持模型列表

| 提供商 | 模型名称 | 默认端点 |
|--------|----------|----------|
| 本地vLLM | qwen/qwen3.6-35b-a3b | http://192.168.0.120:1234/v1 |
| 本地vLLM | qwen/qwen3.6-27b | http://192.168.0.120:1234/v1 |
| 本地vLLM | nvidia/nemotron-3-nano-omni | http://192.168.0.120:1234/v1 |
| 本地vLLM | google/gemma-4-26b-a4b | http://192.168.0.120:1234/v1 |

### 默认配置

- 默认本地大模型地址: `http://192.168.0.120:1234/v1`
- 默认模型: `google/gemma-4-26b-a4b`
- 默认 API Key: `key`

### 大模型连接检查机制

实现了连接测试功能：
1. 用户点击"测试连接"按钮
2. 根据当前选择的提供商配置发起测试请求
3. 调用 `/v1/models` 端点验证服务可用性
4. 返回连接状态与可用模型列表

---

## DOM 处理系统

### 核心能力

DOM 服务负责将原始网页转换为 LLM 能够高效理解的结构化表示。

### 处理步骤详解

1. **快照采集**: 通过 CDP 获取页面完整 DOM
2. **清理过滤**: 移除隐藏元素、不可交互元素
3. **可访问性树构建**: 生成语义化的层次结构
4. **元素索引**: 为所有可交互元素分配唯一 ID
5. **优化序列化**: 输出简洁的 LLM 友好格式

### 输出示例结构

```
[1] <button> 提交表单
[2] <input> 搜索框
[3] <a> 首页链接
...
```

---

## GUI 架构

**文件位置**: `browser_use_gui.py`

### 架构设计原则

采用线程分离架构，确保 GUI 响应性与异步任务执行互不阻塞。

### 主要组件

#### 1. 配置选项卡

功能包括：
- LLM 提供商下拉选择
- API 端点地址配置
- API Key 输入
- 模型名称下拉选择
- "测试连接"按钮

#### 2. 任务选项卡

功能包括：
- 任务文本输入区域
- 开始/停止任务按钮
- 实时进度显示
- 结果展示区域

#### 3. 日志输出面板

实时显示执行过程日志，便于调试与追踪。

### 线程分离架构

```
主线程 (Tkinter GUI)
    │
    ├─ 更新界面元素
    ├─ 响应用户点击
    └─ 启动工作线程
          │
          ▼
    工作线程 (后台)
          │
          ├─ 创建 asyncio 事件循环
          ├─ 运行 Agent 主逻辑
          ├─ 执行浏览器自动化
          └─ 通过 queue 与主线程通信
                │
                ▼
         主线程安全更新UI
```

关键实现点：
- 使用 `queue.Queue` 作为跨线程通信通道
- GUI 主线程通过 `after()` 方法定时轮询队列
- 异步操作完全在独立事件循环中执行

---

## 部署与打包

### PyInstaller 配置

用于将整个项目打包为独立 Windows 可执行文件。

#### 关键打包参数

```bash
pyinstaller \
  --onefile \
  --windowed \
  --collect-all browser_use \
  --hidden-import psutil \
  --hidden-import browser_use.llm.models \
  --name BrowserUse \
  browser_use_gui.py
```

参数说明：
- `--onefile`: 打包为单个 exe 文件
- `--windowed`: 不显示控制台窗口
- `--collect-all browser_use`: 收集 browser_use 包的所有资源
- `--hidden-import`: 显式添加缺失的模块导入

### 常见打包问题与解决方案

| 错误信息 | 解决方案 |
|----------|----------|
| No module named 'psutil' | 添加 `--hidden-import psutil` |
| No module named 'browser_use.llm.models' | 添加 `--hidden-import browser_use.llm.models` 并使用 `--collect-all browser_use` |
| Cannot import name 'get_llm_by_name' | 在 `browser_use/llm/__init__.py` 中正确导出工厂函数 |
| Event handler timed out | 使用临时用户配置目录，添加浏览器启动参数 `--no-first-run` 和 `--no-default-browser-check` |

---

## 开发指南

### 项目结构概览

```
browser-use/
├── browser_use/
│   ├── agent/
│   │   └── service.py          # Agent核心逻辑
│   ├── browser/
│   │   └── session.py          # 浏览器会话管理
│   ├── dom/
│   │   └── service.py          # DOM处理服务
│   ├── llm/
│   │   ├── base.py             # LLM抽象基类
│   │   ├── models.py           # 模型工厂函数
│   │   └── __init__.py         # 模块导出
│   └── ...
├── tests/
│   ├── ci/
│   │   ├── test_agent_planning.py      # Agent规划测试
│   │   └── test_chiphell_first_post.py # 端到端测试
│   └── ...
└── browser_use_gui.py         # GUI主程序入口
```

### 新增 LLM 提供商步骤

1. 在 `browser_use/llm/models.py` 中创建新类继承 `BaseChatModel`
2. 实现所有抽象方法: `ainvoke()`, `ainvoke_batch()`, `get_model_info()`
3. 在 `get_llm_by_name()` 工厂函数中添加新模型分支
4. 在 GUI 配置选项卡中更新提供商与模型列表

### 运行端到端测试

项目提供了预定义的本地测试用例，访问 chiphell.com 并提取第一个帖子信息。

**测试文件**: `tests/ci/test_chiphell_first_post.py`

功能验证点：
- 浏览器正常启动与导航
- DOM 处理与可访问性树生成
- LLM 正确理解任务并执行浏览动作
- 成功提取目标数据

### 代码规范

- 使用 async/await 异步编程模式
- 遵循 Pydantic v2 数据验证规范
- 新增导出在对应包的 `__init__.py` 中声明
- 确保 GUI 操作完全在主线程中执行

---

## API 参考

### Agent 类核心API

```python
class Agent:
    def __init__(
        self,
        task: str,
        llm: BaseChatModel,
        browser: Browser | None = None
    ):
        """初始化Agent实例"""
    
    async def run(self, max_steps: int = 100):
        """启动Agent执行任务"""
```

### LLM 接口 API

```python
async def ainvoke(
    self,
    messages: list[dict[str, Any]],
    temperature: float = 0.7,
    max_tokens: int = 4096,
    **kwargs
) -> dict[str, Any]:
    """异步调用大模型获取响应"""
```

### BrowserSession API

```python
class BrowserSession:
    async def navigate(self, url: str):
        """导航到指定URL"""
    
    async def click_element(self, element_index: int):
        """点击指定索引的元素"""
    
    async def input_text(self, element_index: int, text: str):
        """向指定元素输入文本"""
```

---

*文档版本: 1.0*  
*最后更新: 2026-05-10*
